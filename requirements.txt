# Core ML dependencies (CPU-only for cloud)
torch>=2.5.0
torchvision>=0.20.0
numpy>=1.24.0,<2.0.0
scipy>=1.10.0

# Image processing (headless for cloud)
Pillow>=10.0.0
opencv-python-headless>=4.8.0
scikit-image>=0.21.0
matplotlib>=3.7.0

# DeepGaze for saliency prediction (installed from GitHub)
git+https://github.com/matthias-k/DeepGaze.git

# OCR
easyocr>=1.7.0

# API clients
requests>=2.31.0

# Web UI
streamlit>=1.28.0
pandas>=2.0.0
