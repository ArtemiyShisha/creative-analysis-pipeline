# Creative Analysis Pipeline - AI Context

Контекст для AI-ассистентов (Claude, GPT, etc.) для продолжения разработки проекта.

## 🎯 Что это за проект

**Creative Analysis Pipeline** - система автоматического анализа рекламных креативов с использованием:
1. Eye-tracking simulation (DeepGaze IIE)
2. Компьютерное зрение (OCR, OpenCV)
3. AI для детекции и рекомендаций (GPT-4.1, GPT-5.2)

**Цель:** Дать маркетологам объективные данные о том, как пользователи видят их креатив и что можно улучшить.

## 📐 Архитектура

### Финальный pipeline (9 шагов):

```
1. DeepGaze IIE → Saliency Map
   - Генерирует тепловую карту внимания
   - Точность 96-98% (обучена на реальном eye-tracking)

2. EasyOCR → Text Detection
   - Детектирует текстовые блоки с точными координатами
   - Confidence для каждого блока

3. Python Heuristics → Text Classification
   - Группирует текстовые блоки (header, CTA, subheader, etc.)
   - Правила на основе размера, позиции, keywords

4. GPT-4.1 Vision → Visual Elements
   - Детектирует logo, product
   - НЕ детектирует текст (уже найден OCR)

5. OpenCV → CTA Refinement
   - Находит кнопку вокруг CTA текста
   - Fallback: расширяет текстовый bbox

6. Python → Merge Zones
   - Объединяет text + visual zones
   - Сохраняет nested relationships (subheader внутри product)

7. NumPy → Calculate Attention
   - Накладывает saliency на zones
   - Вычитает вложенные зоны (чтобы не считать дважды)

8. GPT-5.2 + reasoning → Recommendations
   - Анализирует attention distribution
   - Генерирует 3-5 рекомендаций с приоритетами
   - Понимает контекст баннеров (CTA не всегда обязательна)

9. Pillow → Visualization
   - Рисует bbox с цветами по типам зон
   - Подписывает attention %
```

## 🔑 Ключевые решения и почему

### 1. OCR First, не GPT-4.1 First
**Проблема:** GPT-4.1 плохо определяет точные pixel-координаты bbox.
**Решение:** OCR для текста → точные координаты. GPT-4.1 только для визуальных элементов.

### 2. Nested zones не считаются дважды
**Проблема:** Subheader внутри Product → attention считался дважды (coverage >100%).
**Решение:** При расчете Product вычитаем attention вложенных зон.

### 3. CTA refinement через OpenCV
**Проблема:** OCR находит текст "Раскрутите выгоду!", но bbox только для текста, не кнопки.
**Решение:** Ищем темный прямоугольник (кнопку) вокруг текста.

### 4. GPT-5.2 для рекомендаций, не GPT-4.1
**Причина:** GPT-5.2 с `reasoning_effort='medium'` дает более качественные инсайты.

### 5. Banner-aware рекомендации
**Контекст:** Баннер сам кликабелен, CTA кнопка не всегда нужна.
**Решение:** Промпт учитывает, что призыв может быть в header, не только в кнопке.

## 📊 Формат данных

### Zones Structure
```python
{
    'type': str,  # 'logo', 'header', 'cta', 'product', 'subheader', 'description', 'legal'
    'label': str,  # Текст или описание содержимого
    'bbox': [x, y, width, height],  # Координаты
    'attention_pct': float  # Процент внимания (0-100)
}
```

### Results Structure
```python
{
    'image': str,  # Путь к изображению
    'zones': List[Zone],  # Список зон
    'total_zones_attention': float,  # Сумма attention всех зон
    'background_attention': float,  # 100 - total_zones_attention
    'overall_score': float,  # 1.0-5.0
    'reasoning': str,  # Объяснение оценки
    'recommendations': List[Recommendation]  # Рекомендации
}
```

## 🎨 Цветовая схема зон

```python
colors = {
    'logo': (255, 200, 0),      # Yellow
    'header': (0, 200, 100),    # Green
    'subheader': (255, 120, 0), # Orange
    'cta': (100, 150, 255),     # Blue
    'product': (200, 100, 255), # Purple
    'description': (150, 150, 150), # Gray
    'legal': (100, 100, 100)    # Dark Gray
}
```

## 🔧 Текущие ограничения

1. **API costs:** GPT-4.1 + GPT-5.2 на каждый креатив (~$0.05-0.10 per image)
2. **Speed:** ~30-60 секунд на изображение (первый запуск ~2-3 минуты из-за загрузки моделей)
3. **OCR languages:** Сейчас только русский + английский
4. **Визуальная детекция:** GPT-4.1 иногда пропускает product (можно улучшить промпт)
5. **CTA refinement:** Работает только для темных кнопок

## 📈 Что можно улучшить

### Приоритет 1 (быстрые wins):
- [ ] Кэширование saliency map (одно изображение → анализ разных версий разметки)
- [ ] Batch processing (анализ нескольких креативов за раз)
- [ ] Fallback для визуальных зон (если GPT-4.1 не находит product)

### Приоритет 2 (качество):
- [ ] Улучшить промпт GPT-4.1 для product detection
- [ ] Добавить детекцию "person" зоны (лица привлекают внимание)
- [ ] Adaptive thresholds для OCR (сейчас hardcoded)

### Приоритет 3 (UX):
- [ ] Web UI (Gradio/Streamlit)
- [ ] API endpoint (FastAPI)
- [ ] PDF export результатов

### Приоритет 4 (advanced):
- [ ] A/B comparison (сравнение 2 креативов)
- [ ] Time-series analysis (как менялся attention при изменениях)
- [ ] Custom zone types (пользователь задает свои типы)

## 🐛 Known Issues

1. **Юридический текст:** Иногда OCR его находит, но он фильтруется в merge (это ок).
2. **Product detection:** GPT-4.1 иногда пропускает product, если он не визуально доминирующий.
3. **CTA refinement:** Если кнопка не темная, fallback просто расширяет bbox на +20px.
4. **Coverage >100%:** Если забыли вычесть nested zones при расчете attention.

## 🧪 Как тестировать изменения

### 1. Unit тесты (будущее)
```bash
pytest tests/
```

### 2. Manual testing
```bash
# Протестировать на известных креативах
python analyze_creative_final.py data/yandex_pay.png
python analyze_creative_final.py data/yandex_market.png

# Проверить expected values:
# - yandex_pay: Score 3.4, Header 47.3%, Product 19.6%, CTA 5.2%
# - yandex_market: Score 3.4, Header 35.3%, Product 20.1%, Logo 18.5%
```

### 3. Visual inspection
Сравнить `*_final.jpg` - bbox должны быть точными.

## 📚 Полезные контексты

### DeepGaze
- Модель: DeepGazeIIE
- Dataset: MIT1003, SALICON
- Output: Log-density → exp() → probability
- Cache: ~/.cache/torch/hub/

### EasyOCR
- Languages: ['ru', 'en']
- GPU: False (медленнее, но работает везде)
- Confidence threshold: 0.3

### OpenAI API
- GPT-4.1: max_tokens (не max_completion_tokens)
- GPT-5.2: max_completion_tokens + reasoning_effort
- Temperature: GPT-5.2 не поддерживает (только default=1)

## 🔐 Безопасность

- **API keys:** В `config.py` (в .gitignore)
- **Never commit:** config.py с реальными ключами
- **Fallback:** Если config.py нет, читаем из env variable

## 💡 Tips для разработки

1. **Debugging:** Добавь `print(f"Debug: {variable}")` в каждый шаг pipeline
2. **Визуализация:** Сохраняй промежуточные результаты как images для проверки
3. **Профилирование:** Используй `time.time()` для измерения каждого шага
4. **Cache:** DeepGaze и EasyOCR кэшируют модели - первый запуск долгий

## 📞 Вопросы для AI-ассистента

Если ты AI и работаешь над этим проектом, задавай себе:
1. **Зачем?** Какую проблему решаем этим изменением?
2. **Trade-offs?** Что теряем (скорость/качество/стоимость)?
3. **Breaking changes?** Ломаем ли существующий API/формат?
4. **Testing?** Как проверить, что не сломали?

## 🚀 Типичные задачи

### "Добавить новый тип зоны"
1. Обновить эвристику группировки (step 3)
2. Добавить цвет в colors dict (step 9)
3. Обновить промпт GPT-5.2 с новым типом
4. Добавить в README типы зон

### "Улучшить детекцию CTA"
1. Проверить логику в refine_cta_bbox()
2. Добавить больше keywords в classification
3. Попробовать YOLO для детекции кнопок

### "Ускорить pipeline"
1. Профилировать - найти узкое место
2. Кэшировать saliency map
3. Параллелить OCR + DeepGaze

---

**Последнее обновление:** 2026-01-20
**Версия pipeline:** 1.0 (финальная)
